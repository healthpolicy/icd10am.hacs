% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify_hacs.R
\name{identify_hacs}
\alias{identify_hacs}
\title{Identify Hospital Acquired Complications (HACs)}
\usage{
identify_hacs(
  df,
  id,
  format = c("long", "wide"),
  version = NULL,
  df_diag = NULL,
  df_proc = NULL,
  diag = "diag",
  diagno = "diagno",
  cof = NULL,
  proc = "proc",
  diag_prefix = "diag",
  proc_prefix = "proc",
  cof_prefix = NULL,
  drg = "drg",
  icd10am_version = 13L,
  caretype = "caretype",
  qualified_days = "qdays",
  admmode = "admmode",
  los = "los",
  sameday = "samedayf",
  verbose = FALSE
)
}
\arguments{
\item{df}{A data frame with one row per episode.}

\item{id}{The episode identifier column (unquoted name).}

\item{format}{Input format: \code{"long"} (default) or \code{"wide"}.}

\item{version}{HAC specification version string (e.g. \code{"3.2.2"}). Defaults
to \code{NULL}, which selects the latest available version. Earlier versions
remain available for reproducibility.}

\item{df_diag}{Long-format diagnosis data frame. Required when
\code{format = "long"}.}

\item{df_proc}{Long-format procedure data frame. Required when
\code{format = "long"}.}

\item{diag}{Name of the diagnosis code column in \code{df_diag} (quoted string).
Default: \code{"diag"}. Used only when \code{format = "long"}.}

\item{diagno}{Name of the diagnosis sequence number column in \code{df_diag}
(quoted string). Default: \code{"diagno"}. Used only when \code{format = "long"}.}

\item{cof}{Name of the condition onset flag column in \code{df_diag} (quoted
string), or \code{NULL} if the COF is embedded as the first character of each
diagnosis code. Default: \code{NULL}. Used only when \code{format = "long"}.}

\item{proc}{Name of the procedure code column in \code{df_proc} (quoted string).
Default: \code{"proc"}. Used only when \code{format = "long"}.}

\item{diag_prefix}{Prefix shared by all diagnosis columns in \code{df} (quoted
string). Default: \code{"diag"}. Used only when \code{format = "wide"}.}

\item{proc_prefix}{Prefix shared by all procedure columns in \code{df} (quoted
string). Default: \code{"proc"}. Used only when \code{format = "wide"}.}

\item{cof_prefix}{Prefix shared by condition onset flag columns in \code{df}
(quoted string), or \code{NULL} if COF is embedded as the first character of
each diagnosis code. Default: \code{NULL}. Used only when \code{format = "wide"}.}

\item{drg}{Name of the DRG column in \code{df} (quoted string). Default: \code{"drg"}.}

\item{icd10am_version}{ICD-10-AM edition used for coding (integer).
Default: \code{13L}.}

\item{caretype}{Name of the care type column in \code{df} (quoted string).
Default: \code{"caretype"}.}

\item{qualified_days}{Name of the qualified days column in \code{df} (quoted
string). Default: \code{"qdays"}.}

\item{admmode}{Name of the admission mode column in \code{df} (quoted string).
Default: \code{"admmode"}.}

\item{los}{Name of the length of stay column in \code{df} (quoted string).
Default: \code{"los"}.}

\item{sameday}{Name of the same-day flag column in \code{df} (quoted string).
Default: \code{"samedayf"}.}

\item{verbose}{Logical. If \code{TRUE}, print a stage-by-stage processing summary
to the console via \code{message()}. Default: \code{FALSE}.}
}
\value{
The input \code{df} with additional columns:
\itemize{
\item One binary flag per HAC sub-category (\verb{cat*})
\item One summary flag per HAC category (\code{hac1}--\code{hac16})
\item \code{hac15p1}, \code{hac15p2} -- sub-flags for 3rd/4th degree perineal tear
\item \code{hac_array} -- comma-separated string of all detected HAC categories
(e.g. \code{"2,15p1"}). \code{NA} for episodes with no HAC.
}
}
\description{
Identifies Hospital Acquired Complications (HACs) in admitted patient
episodes coded with ICD-10-AM, using the Australian Commission on Safety
and Quality in Health Care (ACSQHC) HAC classification.

Both wide-format and long-format inputs are supported via the \code{format}
argument.
}
\examples{
\dontrun{
library(icd10am.utils)

df_hac <- transform(dfbase,
  drg = "A01A", caretype = "1", qdays = 2L,
  admmode = 2L, los = 5L, samedayf = 0L
)

result <- identify_hacs(
  df             = df_hac,
  id             = recordID,
  format         = "long",
  df_diag        = dfdiag,
  df_proc        = dfproc,
  diag           = "diag",
  diagno         = "diagno",
  cof            = "COF",
  proc           = "proc",
  icd10am_version = 13L,
  verbose        = TRUE
)
}
}
\references{
Australian Commission on Safety and Quality in Health Care.
\emph{Hospital Acquired Complications}. Sydney: ACSQHC.
\url{https://www.safetyandquality.gov.au/our-work/patient-safety/hospital-acquired-complications}
}
